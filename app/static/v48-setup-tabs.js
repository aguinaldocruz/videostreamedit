(function () {
  const setup = $('#setup');
  const pageTitle = setup.querySelector('.page-title');
  const plex = setup.querySelector('.plex-setup');
  const movieImport = $('#movie-import-settings');
  const savedProperties = $('#saved-property-maintenance');
  const templates = $('#template-maintenance');
  const prompts = $('#prompt-settings');
  const indexMaintenance = setup.querySelector('.index-maintenance');
  if (!pageTitle || !plex) return;

  pageTitle.querySelector('h2').textContent = 'Setup';
  pageTitle.querySelector('p').textContent = 'Configure Plex, movie import, reusable metadata, templates, and editing automation.';
  pageTitle.insertAdjacentHTML('afterend', `<div class="setup-tabs" role="tablist" aria-label="Setup sections">
    <button type="button" role="tab" data-setup-tab="plex">Plex</button><button type="button" role="tab" data-setup-tab="import">Movie import</button><button type="button" role="tab" data-setup-tab="properties">Saved properties</button><button type="button" role="tab" data-setup-tab="templates">Templates</button><button type="button" role="tab" data-setup-tab="automation">Automation</button><button type="button" role="tab" data-setup-tab="suggestions">Learned suggestions</button><button type="button" role="tab" data-setup-tab="language-detection">Language detection</button>
  </div><div class="setup-tab-panels">
    <section data-setup-panel="plex"></section><section data-setup-panel="import"></section><section data-setup-panel="properties"></section><section data-setup-panel="templates"></section><section data-setup-panel="automation"></section><section data-setup-panel="suggestions"></section><section data-setup-panel="language-detection"></section>
  </div>`);

  const panel = name => setup.querySelector(`[data-setup-panel="${name}"]`);
  panel('plex').append(plex);
  if (movieImport) panel('import').append(movieImport);
  if (savedProperties) panel('properties').append(savedProperties);
  if (templates) panel('templates').append(templates);
  if (prompts) panel('automation').append(prompts);
  if (indexMaintenance) panel('automation').append(indexMaintenance);
  setup.querySelectorAll('.import-setup-grid').forEach(grid => {if (!grid.children.length) grid.remove()});
  panel('suggestions').innerHTML = `<article id="learned-suggestion-maintenance"><div class="learned-suggestion-heading"><div><h3>Learned track-name suggestions</h3><p>Edit, pause, resume, reset, or remove individual audio and subtitle correction rules.</p></div><button type="button" id="clear-learned-suggestions" class="danger">Delete all</button></div><input type="search" id="learned-suggestion-search" class="search" placeholder="Filter old or replacement names…"><div id="learned-suggestion-list"></div></article>`;
  panel('language-detection').innerHTML = `<article class="language-detection-settings"><div class="detector-section subtitle-detector-section"><div class="detector-section-heading"><div><h3>Subtitle inspection</h3><p>Inspect only configured common-language subtitles. Results are incremental, explainable, and targeted to changed streams.</p></div><span class="detector-badge">Targeted by default</span></div><h4>Common languages</h4><p>Only subtitles whose metadata matches one of these common language bases (or is empty/undetermined) are inspected. The local detector uses built-in vocabulary profiles for Portuguese and English; other configured languages are retained for filtering but are not deeply analyzed.</p><label>Common languages<input id="language-detection-common" type="text" placeholder="pt, pt-BR, en" autocomplete="off"></label><button type="button" class="primary" id="language-detection-save">Save common languages</button><small id="language-detection-status"></small></div><div class="detector-section voice-detector-section"><div class="detector-section-heading"><div><h3>Voice language detection</h3><p>Optional audio checks using the separate SpeechBrain service. Voice detection is independent from subtitle inspection.</p></div><span class="detector-badge">Optional</span></div><label class="voice-setting-toggle"><input id="voice-detection-enabled" type="checkbox"> Enable voice detection interface</label><div id="voice-detection-config"><label>Service URL<input id="voice-detection-url" type="text" placeholder="http://language-id:9000"></label><label>Sample seconds<input id="voice-detection-seconds" type="number" min="10" max="120" step="1"></label><label>Number of samples<input id="voice-detection-count" type="number" min="1" max="9" step="1"></label><label>Sample positions<input id="voice-detection-positions" type="text" placeholder="0.1, 0.5, 0.9"></label></div><button type="button" class="primary" id="voice-detection-save">Save voice settings</button><button type="button" id="voice-detection-help">Docker instructions</button><small id="voice-detection-status"></small></div><div class="detector-section queue-detector-section"><div class="detector-section-heading"><div><h3>Detection automation</h3><p>Changed-media detection is the normal mode. A full catalog run is an advanced maintenance action and is not started automatically.</p></div><span class="detector-badge">Non-blocking queue</span></div><div class="language-detection-queue-actions"><button type="button" class="primary" id="language-detection-queue-full">Queue full catalog (advanced)</button><button type="button" id="language-detection-queue-incremental">Enable changed-media detection</button></div><label class="voice-setting-toggle"><input id="language-detection-incremental" type="checkbox"> Automatically queue detection after media changes</label><small id="language-detection-queue-status"></small><div id="subtitle-detection-health" class="vse-status-panel detector-health" aria-live="polite"><strong>Subtitle inspection health</strong><span>Loading detector status…</span></div></div><div class="detector-section secondary-detector-section"><h3>Other report configuration</h3><p>These settings affect reports only; they do not trigger subtitle inspection.</p><hr><h4>Forced streams report exclusions</h4><p>Manage track names to omit from the Forced streams report. Matching is case-insensitive.</p><div class="forced-exclusion-add"><input id="forced-report-exclusion-new" type="text" placeholder="Track name to exclude" autocomplete="off"><button type="button" id="forced-report-exclusion-add" class="primary">Add track name</button></div><div id="forced-report-exclusion-list"></div><small id="forced-report-exclusions-status"></small><hr><h3>Duplicate-language reports</h3><p>Reports list media with more than one audio or subtitle stream in the same configured language.</p><label>Audio languages<input id="duplicate-audio-languages" type="text" placeholder="en, pt"></label><label>Subtitle languages<input id="duplicate-subtitle-languages" type="text" placeholder="en, pt"></label><button type="button" class="primary" id="duplicate-language-save">Save duplicate report languages</button><small id="duplicate-language-status"></small><hr><h3>OCR conversion staging</h3><p>Converted media is placed at its final media path for review. The original remains staged until approval; if you reject the conversion, rollback restores the staged original. Approval only removes rollback and temporary data.</p><small id="ocr-stage-root" class="muted"></small><div id="ocr-staged-list"><p class="muted">Loading staged conversions…</p></div><hr><h3>Voice metadata report</h3><p>Audio streams whose sampled spoken language disagrees with the stored stream language.</p><button type="button" class="primary" id="audio-language-report-open">Refresh voice report</button><small id="audio-language-report-status"></small><div id="audio-language-report-list"><p class="muted">No report loaded.</p></div></div></article>`;

  const TAB_KEY = 'videostreamedit.setup-tab.v1';
  function activateSetupTab(name) {
    if (!panel(name)) name = 'plex';
    setup.querySelectorAll('[data-setup-tab]').forEach(button => {const active=button.dataset.setupTab===name;button.classList.toggle('active',active);button.setAttribute('aria-selected',String(active))});
    setup.querySelectorAll('[data-setup-panel]').forEach(item => item.classList.toggle('hidden',item.dataset.setupPanel!==name));
    localStorage.setItem(TAB_KEY,name);
    if (name === 'suggestions') loadLearnedSuggestions();
    if (name === 'language-detection') loadOcrStaged();
  }
  setup.querySelectorAll('[data-setup-tab]').forEach(button => button.onclick=()=>activateSetupTab(button.dataset.setupTab));
  activateSetupTab(localStorage.getItem(TAB_KEY)||'plex');

  let learnedSuggestions=[];
  function suggestionPayload(item, action, replacement) {return{stream_type:item.stream_type,track_language:item.track_language||"",old_value:item.old_value,new_value:item.new_value,action,replacement}}
  function renderLearnedSuggestions() {
    const query=$('#learned-suggestion-search').value.trim().toLowerCase(),items=learnedSuggestions.filter(item=>!query||`${item.old_value} ${item.new_value} ${item.stream_type}`.toLowerCase().includes(query));
    $('#learned-suggestion-list').innerHTML=items.length?items.map((item,index)=>`<div class="learned-suggestion-item ${item.enabled?'':'paused'}" data-suggestion-index="${learnedSuggestions.indexOf(item)}"><span class="stream-type-badge">${esc(item.stream_type)}<small>${esc(item.track_language || "und")}</small></span><div><small>Current value</small><strong>${esc(item.old_value||'(empty)')}</strong></div><span class="suggestion-arrow">→</span><label><small>Suggested replacement</small><input type="text" value="${attr(item.new_value)}"></label><span class="suggestion-count">${item.use_count} uses${item.enabled?'':' · paused'}</span><div class="suggestion-actions"><button type="button" data-save-suggestion>Save</button><button type="button" data-toggle-suggestion>${item.enabled?'Pause':'Resume'}</button><button type="button" data-reset-suggestion>Reset</button><button type="button" class="danger" data-delete-suggestion>Delete</button></div></div>`).join(''):'<p class="muted">No learned suggestions match.</p>';
    $('#learned-suggestion-list').querySelectorAll('.learned-suggestion-item').forEach(row=>wireSuggestionRow(row));
  }
  async function updateSuggestion(item,action,replacement){const result=await api('/api/v59/settings/track-name-suggestions',{method:'PUT',body:JSON.stringify(suggestionPayload(item,action,replacement))});learnedSuggestions=result.suggestions;renderLearnedSuggestions()}
  function wireSuggestionRow(row){const item=learnedSuggestions[Number(row.dataset.suggestionIndex)],input=row.querySelector('input');row.querySelector('[data-save-suggestion]').onclick=()=>updateSuggestion(item,'rename',input.value).then(()=>toast('Learned replacement updated')).catch(error=>toast(error.message,true));row.querySelector('[data-toggle-suggestion]').onclick=()=>updateSuggestion(item,item.enabled?'pause':'resume').then(()=>toast(item.enabled?'Suggestion paused':'Suggestion resumed')).catch(error=>toast(error.message,true));row.querySelector('[data-reset-suggestion]').onclick=()=>updateSuggestion(item,'reset').then(()=>toast('Suggestion usage reset')).catch(error=>toast(error.message,true));row.querySelector('[data-delete-suggestion]').onclick=async()=>{if(!confirm(`Delete the learned correction “${item.old_value||'(empty)'}” → “${item.new_value}”?`))return;try{const result=await api('/api/v59/settings/track-name-suggestions',{method:'DELETE',body:JSON.stringify(item)});learnedSuggestions=result.suggestions;renderLearnedSuggestions();toast('Learned suggestion deleted')}catch(error){toast(error.message,true)}}}
  async function loadLearnedSuggestions(){try{const result=await api('/api/v59/settings/track-name-suggestions');learnedSuggestions=result.suggestions;renderLearnedSuggestions()}catch(error){$('#learned-suggestion-list').innerHTML=`<p class="error">${esc(error.message)}</p>`}}

  function renderOcrStaged(items) {
    const root=$('#ocr-staged-list');
    if (!items.length) {root.innerHTML='<p class="muted">No OCR staging backups awaiting review.</p>';return;}
    root.innerHTML=items.map(item=>`<div class="ocr-staged-row" data-id="${item.id}"><div><strong>${esc(item.title||item.original_path)}</strong><small>${esc(item.media_kind||'media')} · ${esc(item.original_path)} · ${esc(item.status)} · original backup ${Math.max(0,Math.round((Number(item.size_bytes)||0)/1048576))} MB</small></div><div class="ocr-staged-actions">${item.converted_path?`<a class="button" href="/api/v68/ocr/staged/${item.id}/converted" target="_blank" rel="noopener">Review converted</a>`:''}${item.converted_path&&item.converted_path!==item.original_path?'<button type="button" data-ocr-finalize>Move converted to media path</button>':''}<button type="button" data-ocr-rollback>Rollback to original</button><button type="button" class="danger" data-ocr-approve>Approve &amp; remove rollback data</button></div></div>`).join('');
    root.querySelectorAll('.ocr-staged-row').forEach(row=>{
      const id=row.dataset.id;
      row.querySelector('[data-ocr-finalize]')?.addEventListener('click',async()=>{if(!confirm('Move the converted candidate to the media’s original path? The original backup will remain available for rollback until approved.'))return;try{await api(`/api/v68/ocr/staged/${id}/finalize`,{method:'POST'});toast('Converted media is now at the original media path');loadOcrStaged()}catch(error){toast(error.message,true)}});
      row.querySelector('[data-ocr-rollback]').onclick=async()=>{if(!confirm('Restore the original media from staging? The staged backup will be kept until approval.'))return;try{const result=await api(`/api/v68/ocr/staged/${id}/rollback`,{method:'POST'});toast(`Rollback queued as task #${result.task_id}`);loadOcrStaged()}catch(error){toast(error.message,true)}};
      row.querySelector('[data-ocr-approve]').onclick=async()=>{if(!confirm('Approve this conversion and delete the staged original/rollback data? The media at its original path will be kept.'))return;try{await api(`/api/v68/ocr/staged/${id}/approve`,{method:'POST'});toast('Rollback data removed; final media retained');loadOcrStaged()}catch(error){toast(error.message,true)}};
    });
  }
  async function loadOcrStaged(){try{const result=await api('/api/v68/ocr/staged');$('#ocr-stage-root').textContent=`Staging area: ${result.stage_root}`;renderOcrStaged(result.items||[])}catch(error){$('#ocr-staged-list').innerHTML=`<p class="error">${esc(error.message)}</p>`}}
  $('#learned-suggestion-search').oninput=renderLearnedSuggestions;
  $('#clear-learned-suggestions').onclick=async()=>{if(!confirm('Delete all learned track-name suggestions? This cannot be undone.'))return;try{await api('/api/v59/settings/track-name-suggestions/all',{method:'DELETE'});learnedSuggestions=[];renderLearnedSuggestions();toast('All learned suggestions deleted')}catch(error){toast(error.message,true)}};

  const tabbedLoadRoots=loadRoots;
  loadRoots=async function(){await tabbedLoadRoots();if((localStorage.getItem(TAB_KEY)||'plex')==='suggestions')await loadLearnedSuggestions()};
})();
